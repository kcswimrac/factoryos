node_modules/
.env
.DS_Store

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Runtime artifacts
debug/
shell-snapshots/
todos/
.claude.json.backup.*

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl

# Polsia safety (auto-injected)
.env
.env.*
.claude/
projects/
*.jsonl
